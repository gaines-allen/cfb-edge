name: Daily update

# What runs here is only the deterministic half of the system: pull the
# FanDuel board, settle anything that finished, rebuild the page, commit.
# Making the picks needs an agent doing actual research, so that runs on
# Wednesday from Claude, not from here.

on:
  schedule:
    # 07:00 America/Chicago, every day. Line movement plus overnight grading.
    - cron: "0 12 * * *"
    # Saturday 10:00 and 12:00 Chicago. These two runs are what capture
    # closing lines before the noon and afternoon kicks, so they matter more
    # than any other pull in the week.
    - cron: "0 15 * * 6"
    - cron: "0 17 * * 6"
    # Sunday 09:00 Chicago. Everything from Saturday is final by now.
    - cron: "0 14 * * 0"
  workflow_dispatch:
    inputs:
      markets:
        description: "Market set to pull"
        required: false
        default: "default"
        type: choice
        options: [default, all]
      season:
        description: "Season to grade"
        required: false
        default: "2026"

permissions:
  contents: write

concurrency:
  group: daily-update
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - run: pip install -r requirements.txt

      # These two need API keys and reach out to the network, so a bad day
      # upstream must not stop the page from rebuilding and deploying. A
      # stale board on a live page beats a dead page.
      - name: Pull the FanDuel board
        id: odds
        continue-on-error: true
        env:
          ODDS_API_KEY: ${{ secrets.ODDS_API_KEY }}
        run: |
          python3 scripts/fetch_odds.py --markets "${{ inputs.markets || 'default' }}"

      - name: Grade anything that finished
        id: grade
        continue-on-error: true
        env:
          CFBD_API_KEY: ${{ secrets.CFBD_API_KEY }}
        run: |
          python3 scripts/grade_results.py --season "${{ inputs.season || '2026' }}"

      - name: Check the grading math
        run: python3 scripts/selftest.py

      - name: Report what failed
        if: steps.odds.outcome == 'failure' || steps.grade.outcome == 'failure'
        run: |
          echo "### Upstream step failed" >> $GITHUB_STEP_SUMMARY
          echo "Odds pull: ${{ steps.odds.outcome }}" >> $GITHUB_STEP_SUMMARY
          echo "Grading: ${{ steps.grade.outcome }}" >> $GITHUB_STEP_SUMMARY
          echo "Most often this means ODDS_API_KEY or CFBD_API_KEY is missing from repository secrets, or the monthly credit quota is spent." >> $GITHUB_STEP_SUMMARY

      - name: Rebuild the page
        run: python3 scripts/build_site.py

      - name: Commit
        run: |
          git config user.name "cfb-edge-bot"
          git config user.email "actions@github.com"
          git add data site
          if git diff --staged --quiet; then
            echo "Nothing moved."
          else
            git commit -m "Daily update: $(date -u +'%Y-%m-%d %H:%M UTC')"
            git push
          fi

  deploy:
    needs: update
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site
      - id: deployment
        uses: actions/deploy-pages@v4
