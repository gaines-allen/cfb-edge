---
description: Sunday. Settle the week, rebuild the learning surfaces, write the honest review.
---

Hand off to grader. It settles every pending pick against the final score,
recomputes record, ROI and closing line value, rebuilds the calibration table
and the factor scorecard, and writes at most three actionable lessons into
data/memory.json.

Make sure it does the part that is easy to skip: go back through the losses
and separate the picks that were wrong from the picks that were right and
lost anyway, by checking each rationale's stated risk against what actually
happened. A pick that lost the way it said it might is a different animal
from a pick that lost for a reason nobody considered.

Then hand off to site-builder to rebuild the page, and commit.

Report the week in units, the running season number, average CLV, and the one
finding that should change how next week's card gets made.

$ARGUMENTS
